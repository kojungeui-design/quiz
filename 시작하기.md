# 배터리 카달로그 — 이 폴더에서 관리한다

지금까지 만든 게 전부 이 폴더 하나에 있다.
**폴더를 통째로 원하는 자리에 두고, 앞으로는 여기서만 작업한다.**

권장 위치 (백업이 도는 자리):

```
D:\배터리카달로그\
문서\배터리카달로그\
```

> ⚠️ **바탕화면\임시 같은 데 두지 마라.** 앱 데이터가 브라우저에 붙어 있어서
> 폴더를 옮기면 꼬일 수 있다. 자리를 정했으면 그대로 둔다.
> 꼭 옮겨야 하면 **옮기기 전에 백업부터 내보낸다**(아래 3번).

---

## 폴더 안에 뭐가 있나

| | 무엇 | 어떻게 |
|---|---|---|
| 📄 | **`battery_catalog.html`** | **더블클릭** — 앱이 열린다. 이게 본체다 |
| ⚙️ | **`실행하기_윈도우.bat`** | **더블클릭** — 카달로그 수집기 메뉴 |
| 📊 | `글로벌_SLI_배터리_카달로그.xlsx` | 같은 데이터를 엑셀 12시트로 |
| 📁 | `data\` | 카달로그 주소 77건 · 유통사 44곳 (엑셀로 열림) |
| 📁 | `tools\` | 파이썬 도구들. 직접 건드릴 일 없다 |
| 📁 | **`catalogs\`** | **받은 카달로그 PDF 가 여기 쌓인다** |
| 📁 | **`내보내기\`** | **앱 백업을 여기 저장한다** ← 제일 중요 |
| 📁 | `스킬\` | AI 에게 이걸 다시 시킬 때 쓰는 문서 |
| 📖 | `내일_회사에서_할일.md` | 단계별 실행 안내 |

---

## 1. 이미 받아둔 카달로그가 있으면 — 합치기

전에 압축 푼 폴더에 `catalogs\` 가 이미 있으면 **그걸 이 폴더로 옮겨온다.**

1. 예전 폴더의 `catalogs` 안 내용물을 전부 복사
2. 이 폴더의 `catalogs` 안에 붙여넣기 (덮어쓰기)
3. `실행하기_윈도우.bat` → **`6`** — 지역·브랜드 폴더로 다시 정리된다

그 다음부터 예전 폴더는 지워도 된다.

---

## 2. 평소 작업 순서

```
① battery_catalog.html 더블클릭   → 보고 · 고치고 · 찾는다
② 실행하기_윈도우.bat 더블클릭     → 새 카달로그를 받는다
③ 6번으로 정리                    → catalogs\카달로그\ 에 지역별로 깔린다
④ 앱 → 데이터 관리 → CSV 가져오기  → 뽑은 스펙을 앱에 넣는다
⑤ 앱 → 데이터 관리 → 전체 JSON 백업 → 내보내기\ 에 저장          ★ 매번
```

---

## 3. ★ 백업 — 이것만은 꼭

**앱에서 고친 내용은 이 폴더가 아니라 브라우저 안에 저장된다.**
브라우저 캐시를 지우거나, 다른 PC 에서 열거나, 회사에서 PC 를 밀면 **날아간다.**

그래서 작업이 끝날 때마다:

> 앱 → **데이터 관리** → **전체 JSON 백업** → `내보내기\` 폴더에 저장

파일명은 날짜를 넣어라. `배터리DB_2026-07-29.json`

되살릴 때는 앱 → **데이터 관리** → **JSON 복원** → 그 파일 선택.

**폰과 PC 를 같이 쓰는 방법도 이거다.** PC 에서 백업 → 카톡으로 폰에 보내기 →
폰 앱에서 [JSON 복원]. 반대도 똑같다.

---

## 4. 엑셀을 다시 굽고 싶으면

앱에서 데이터를 많이 고쳤으면 엑셀도 새로 만든다. 명령 프롬프트에서:

```
cd /d D:\배터리카달로그
node tools\dump_seed.js battery_catalog.html seed.json
python tools\build_workbook.py seed.json 글로벌_SLI_배터리_카달로그.xlsx
```

Node.js 가 없으면 이 단계는 건너뛰어도 된다. 앱만으로 다 볼 수 있다.

---

## 5. 남에게 줄 때

| 주려는 것 | 보낼 파일 |
|---|---|
| 앱만 보여주기 | `battery_catalog.html` 하나 (그 자체로 완결) |
| 데이터까지 같이 | 위 + `내보내기\` 의 최신 JSON |
| 엑셀로 보기 | `글로벌_SLI_배터리_카달로그.xlsx` |
| 전부 | 이 폴더를 통째로 압축 |

> ⚠️ `catalogs\` 안의 PDF 는 **제조사 저작물**이다. 사내에서 근거로 쓰는 건
> 괜찮지만 외부에 재배포하지 마라.

---

## 6. 지금 데이터가 어느 정도인지

| | |
|---|---|
| 모델 | 862 — 그중 **81% 가 아직 `라인업슬롯`** (규격 표준값을 복사한 것) |
| 규격 | 49종 |
| 제조사·유통사 | 74 |
| 브랜드 × 마켓 | 76 |
| 유통사 | 44 |
| 카달로그 주소 | 77 (존재 확인 35 · 패턴추정 40) |

**아직 영업·기술 자료로 쓰면 안 된다.** 앱 대시보드가 그렇게 띄운다.
`실행하기_윈도우.bat` 로 카달로그를 받고 그 값으로 덮어야 진짜가 된다.

---

## 막혔을 때

`내일_회사에서_할일.md` 맨 아래 "막혔을 때" 표를 봐라.
그래도 안 되면 검은 창을 폰으로 찍어서 물어보면 된다.
