{
  "name": "battery-design-studio",
  "version": "8.0.0",
  "private": true,
  "type": "module",
  "description": "배터리 신규 설계 자동화 워크벤치 — 의존성 없이 node만으로 빌드되는 오프라인 단일 HTML 도구",
  "scripts": {
    "build": "node build.mjs",
    "build:data": "node data/build-db.mjs && node data/build-db.mjs --masked",
    "test": "node --test test/*.test.mjs"
  }
}
