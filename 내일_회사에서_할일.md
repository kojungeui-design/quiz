# 내일 회사에서 — 순서대로 하나씩

폰으로 보면서 따라 하면 된다. **A 부터 하고, 잘 되면 B 로 간다.**
A 만 해도 앱은 쓸 수 있다.

---

# A. 앱부터 열어본다 (설치 없음 · 3분)

컴퓨터에 아무것도 안 깔아도 된다.

### A-1. 파일 받기

브라우저에서 이 주소로 간다.

```
https://github.com/kojungeui-design/quiz
```

- `claude/lead-acid-battery-catalog-rggvev` 브랜치로 바꾼다
  (왼쪽 위 `main` 이라고 쓰인 버튼 → 목록에서 고른다)
- 초록색 **`Code`** 버튼 → **`Download ZIP`**
- 받은 zip 을 **바탕화면에 압축 해제**한다

> 회사가 GitHub 을 막아뒀으면 → 내가 카톡/메일로 보낸 `battery_catalog.html`
> 하나만 있으면 A 는 된다. 그 파일만 바탕화면에 두면 끝.

### A-2. 열기

`battery_catalog.html` 을 **더블클릭**한다. 브라우저에 앱이 뜬다.
인터넷 없어도 뜬다. 서버도 필요 없다.

### A-3. 확인할 것

| 눌러볼 곳 | 보이면 정상 |
|---|---|
| 대시보드 | 맨 위에 빨간 경고 — "대부분 추정값" |
| 카달로그 | 표가 뜨고, 아무 줄이나 누르면 오른쪽에 상세가 열린다 |
| 세계지도 | 대륙 모양이 보이고 그 위에 동그라미 |
| 유통사·경쟁브랜드 | 44곳. 우리 DB 에 없는 브랜드는 **빨간색** |

여기까지 되면 A 는 끝이다. **이것만으로도 회의에서 보여줄 수 있다.**

---

# B. 카달로그를 실제로 긁어온다 (30분)

여기서부터 진짜 데이터가 들어온다.

### B-1. 더블클릭 한 번

압축 푼 폴더 안에 있는

```
실행하기_윈도우.bat
```

를 **더블클릭**한다. 검은 창이 뜬다.

> "Windows의 PC 보호" 경고가 뜨면 → **추가 정보** → **실행**

### B-2. 파이썬이 없다고 하면

검은 창에 `[X] 파이썬이 없습니다` 가 뜨면:

1. 시작 메뉴 → **Microsoft Store** → `Python` 검색 → 최신 버전 **설치**
2. 설치 끝나면 `실행하기_윈도우.bat` 를 **다시 더블클릭**

Store 가 막혀 있으면 python.org 에서 받되,
설치 첫 화면의 **`Add python.exe to PATH` 체크박스를 꼭 켠다.**

### B-3. 메뉴가 뜨면 `1` 을 넣고 엔터

```
1  주소가 살아있는지 확인 (1~2분, 파일은 안 받음)   ← 이것부터
2  카달로그 전체 수집 (수십 분)
3  PDF 만 수집 (빠름)
4  유통사 사이트에서 경쟁 브랜드 찾기
5  받을 주소 목록만 화면에 보기 (확인도 안 함)
6  받은 카달로그를 폴더로 정리 (지역 > 브랜드)
```

**반드시 `1` 부터.** 파일은 하나도 안 받고, 주소 77개를 하나씩 두드려서
열리는지만 본다. 화면에 이렇게 줄줄 지나간다.

```
  OK  gb_export    200 https://sebang-europe.com/assets/downloads/rocket_...
   X  recor_eu     404 https://eu.recorbatteries.gr/catalogues/
```

### B-4. 결과 읽는 법

**다 끝나면 화면 맨 아래에 답이 바로 나온다.** 파일 안 열어봐도 된다.

```
====================================================
  주소 77개 중 41개가 열린다.
  아주 좋다 — 바로 전체 수집(2번)으로 가도 된다.
  자세한 내역: ...\catalogs\url_check.csv
====================================================
```

| 화면이 이러면 | 다음 |
|---|---|
| **30개 이상** — "아주 좋다" | B-5 로. `2`(전체) 까지 가도 된다 |
| **10~30개** — "쓸 만하다" | B-5 로. `3`(PDF) 부터 |
| **10개 미만** — "너무 적다" | 회사 방화벽이다 → 아래 "막혔을 때" |

주소별로 왜 막혔는지 보려면 `catalogs\url_check.csv` 를 엑셀로 연다.
`결과` 열이 `열림`/`막힘`/`실패` 로 찍혀 있다.

> `collect_log.csv` 는 **1번에서는 안 생긴다.** 2·3·4번을 돌려야 생긴다.

### B-5. 이제 `3` 번 (PDF 만)

`실행하기_윈도우.bat` 를 다시 돌리고 이번엔 **`3`**.
PDF 16개를 받아서 스펙을 뽑는다. 이게 알맹이다.

잘 되면 마지막으로 **`2`** (전체) 를 돌린다. 수십 분 걸리니 점심 먹으러 갈 때 돌려두면 된다.

### B-6. 받은 카달로그를 폴더로 정리

수집기는 `catalogs\bosch_in\` 처럼 **기계용 이름**으로 떨군다.
사람이 찾을 수 있게 다시 깔아준다. `실행하기_윈도우.bat` → **`6`**.

```
catalogs\카달로그\
  05_인도\Bosch\      Bosch_인도_2023_battery_catalogue.pdf
  06_유럽\Bosch\      Bosch_유럽_2017_batteries_s3s4s5_2pp.pdf
          Exide\      Exide_유럽_NEW_Exide_Edition5_TechnicalGuid.pdf
  10_오세아니아\Century\ Century_오세아니아_cross-reference-chart.pdf
  _목록.csv     ← 엑셀. 지역·브랜드·쪽수·SHA-256·원본주소
  _목록.html    ← 더블클릭하면 클릭으로 PDF 가 열린다
```

원본은 그대로 두고 **복사**한다. 여러 번 돌려도 안전하다.

### B-7. 앱에 넣기

1. `catalogs\collected_models.csv` 를 확인한다
2. `battery_catalog.html` 을 연다
3. **데이터 관리** → **CSV 가져오기** → 그 파일을 고른다
4. **카달로그** 뷰에서 근거가 `추정` → `공개스펙` 으로 바뀐 걸 본다

> ⚠️ **자동으로 뽑은 건 그대로 믿으면 안 된다.**
> `확신도` 열이 낮은 줄부터 PDF 원본과 대조해라. 그게 진짜 일이다.

---

# 막혔을 때

| 증상 | 이렇게 |
|---|---|
| `pip install` 이 실패 | IT 팀에 **회사 프록시 주소**를 물어본다. 검은 창이 알려주는 명령에 그 주소를 넣는다 |
| 주소가 거의 다 죽음 | 회사망이 해외 사이트를 막는 거다. **개인 폰 핫스팟**에 노트북을 물려서 다시 돌려본다 |
| GitHub 이 막힘 | 파일을 메일/카톡으로 받는다 |
| bat 파일이 안 켜짐 | 파일 우클릭 → **속성** → 아래쪽 `차단 해제` 체크 → 확인 |
| 검은 창이 순식간에 닫힘 | 실패한 거다. 창 내용을 폰으로 찍어서 나한테 보내라 |

---

# 폰만 있어도 되는 길

컴퓨터가 아예 안 되면 이 방법도 있다. **느리지만 결과물의 질은 같다.**

1. 폰에서 `battery_catalog.html` 을 연다
2. **제조사·수집관리** 에서 카달로그 링크를 누른다 → PDF 가 폰에 저장된다
3. **데이터 관리 → 폰에서 바로 등록** 에서 그 PDF 를 고른다
   → 브라우저가 **SHA-256·페이지수·판·발행연도**를 직접 계산해 근거로 박는다
   (파일이 어디로도 전송되지 않는다. 폰 안에서만 처리된다)
4. PDF 를 보면서 **카달로그** 뷰의 해당 모델 숫자를 고치고 근거를 `공개스펙` 으로 바꾼다

카달로그 하나만 이렇게 해봐도, 나머지 76개가 같은 방식으로 굴러간다는 게 증명된다.

---

# 내일 이것만 되면 성공

- [ ] `battery_catalog.html` 이 열린다
- [ ] `실행하기_윈도우.bat` 에서 **1번**이 돌아간다
- [ ] 화면에 "주소 O개 중 O개가 열린다" 가 떴다

**이 세 개면 충분하다.** 나머지는 그 결과를 보고 정하면 된다.
