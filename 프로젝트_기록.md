# 배터리 카달로그 시스템 — 전체 기록

세방전지 기획 담당이 "전세계 납축전지 카달로그를 모아 성능을 정리·관리하고 싶다"
에서 시작해, 사내 PC 에서 실제로 카달로그를 긁어 스펙을 뽑는 데까지 간 기록이다.

요청이 어떻게 바뀌었고, 무엇을 만들었고, **무엇이 틀렸다가 어떻게 고쳐졌는지**를
순서대로 남긴다. 마지막 절에 지금 상태와 남은 일이 있다.

---

## 1부 — 기획과 뼈대

### 요청이 자란 순서

| # | 요청 | 그래서 무엇이 바뀌었나 |
|---|---|---|
| 1 | 전세계 제조사·유통사 배터리 카달로그를 모아 성능을 정리·관리 | 규격 마스터를 1급 단위로 두는 구조 결정 |
| 2 | 배터리별로 **어떤 차량에 적합한지** DB | `FITMENT` 테이블 신설 (요구 규격·용량·CCA·ISG) |
| 3 | **판매지역**도 구분해서 관리 | 모델에 지역 배열(`rg`), 지역 11개 코드 |
| 4 | 사내 일본 제품 엑셀 참고 | `gb_japan_to_catalog_csv.py` — 사내 엑셀 → 임포트 CSV |
| 5 | **대상은 SLI — AGM·EFB·FLD** | 딥사이클·산업용·UPS 전부 범위에서 제외 |
| 6 | **근거를 남기려 카달로그를 PDF 로 받고 판(edition)도 기록** | SHA-256·쪽수·판·발행연도를 남기는 근거 체계 |
| 7 | 사내 엑셀은 일본뿐 — **다른 지역은 네가 전부 찾아라** | 지역별 제조사·유통사 조사 → 시드 확장 |
| 8 | **폰으로 할 수 있는 모든 것** | 단일 파일 HTML·표를 카드로·하단 탭·Web Share·폰에서 PDF 해시 계산 |
| 9 | HTML **과 엑셀** 둘 다 | 같은 시드에서 12시트 워크북을 굽는 파이프라인 |
| 10 | DB 가 부족하다 · **카달로그 링크가 대표 홈페이지로 간다** · Bosch 는 국가마다 모델이 다른데 정보가 없다 | ① 실제 카달로그 주소 레지스트리 ② **브랜드 × 마켓**을 1급 단위로 승격 |
| 11 | 사내 바이어 DB(HTML) 를 가져와 더 찾아라 | gzip+base64 해제 → 46,763행에서 제품형명 × 국가 추출 |
| 12 | **세계지도가 안 보인다** | 대륙 폴리곤을 넣어 산점도를 지도로 |
| 13 | 바이어는 남의 배터리도 받는다 — **그 정보로 다시 검색해서 더 완성된 정보를** | 유통사를 경쟁 브랜드 발굴 경로로 재정의 |
| 14 | 다 네가 찾아봐 | 바이어 하나씩 조사 → 유통사 29 → 44곳 |
| 15 | GPT 로도 똑같이 시키겠다 — **스킬과 프롬프트로** | `.claude/skills/battery-catalog/` + `GPT_PROMPT.md` |
| 16 | 내일 회사에서 해볼 테니 **쉽게 하나하나** | 더블클릭 실행기 + 단계별 안내서 |
| 17 | 로컬 PC 폴더 하나에서 관리하고 싶다 | 폴더 패키지 + 백업 규칙 |
| 18 | **사내자료를 더 추가했다** — 네가 조사한 거에 다 있냐 | 대조표 + 사내 파일을 직접 읽는 도구 |

### 이 과정에서 굳어진 설계 원칙

**규격 마스터에서 치수를 물려받는다.** 모델 행은 스펙만 갖고 L×W×H 는 규격에서
채운다. 그래야 같은 규격끼리 비교와 차량 매칭이 자동으로 붙는다.

**점수는 용도군 안에서만.** 승용 SLI 와 상용차를 한 축에 놓고 점수를 매기면
의미가 없다. 같은 `app` 그룹 안에서만 0~100 으로 정규화한다.

**EN 과 SAE 는 다른 시험이다.** EN 은 10초 후 7.5V, SAE 는 30초 후 7.2V.
정확한 환산식이 없어서 원 표기값과 표기 규격을 항상 같이 저장하고, 비교할 때만
근사 계수(기본 0.95)를 쓰되 화면에 "환산값" 을 남긴다.

**브랜드 × 마켓이 1급 단위다.** Bosch 는 유럽 S3~S6 + DIN/ETN, 북미 BCI,
인도 S4+/T4/M6, 중국 저상 L2 다. 브랜드 하나로 뭉치면 그 차이가 사라지고
"Bosch 정보가 없다" 는 착시가 생긴다. 실제로 이 요청의 원인이 그거였다.

**브랜드 소유자 ≠ 제조 주체.** 중동 ACDelco 는 ACDelco 가 아니라 사우디
MEBCO(Clarios 49% 합작)가 만든다. `mk` 는 실제 만드는 곳을 가리켜야 한다.

**추정을 사실로 쓰지 않는다.** 모든 행에 근거 등급을 단다 —
`std`(규격표준) / `pub`(공개스펙) / `est`(추정) / `slot`(라인업슬롯) /
`own`(사내설계치) / `user`(직접입력). 슬롯·추정이 다수면 대시보드가 직접 경고한다.

**못 찾은 건 못 찾았다고 남긴다.** 바이어·URL 을 그럴싸하게 지어내면 근거 체계
전체가 무너진다.

---

## 2부 — 만든 것

### 앱 — `battery_catalog.html`

외부 자원을 하나도 쓰지 않는 단일 파일. `file://` 로도 돌고 폰에서도 돈다.
11개 화면:

대시보드 · 카달로그 · 차량 적합성 · 성능 비교 · 규격 크로스레퍼런스 ·
세계지도 · 판매지역 · 브랜드·마켓 · 유통사·경쟁브랜드 · 제조사·수집관리 ·
데이터 관리

### 엑셀 — 12시트 워크북

읽어보기 / 설정 / 규격마스터 / **카달로그** / 제조사 / 브랜드마켓 / 유통사 /
차량적합성 / 지역커버리지 / **지역별판매** / 사이즈별요약 / 크로스레퍼런스

파생지표와 집계는 값이 아니라 **수식**이라 스펙을 고치면 갭·커버리지가 따라 움직인다.

### 도구 — `tools/`

| 파일 | 하는 일 |
|---|---|
| `collect.py` | 카달로그 수집 엔진. `--check` `--dry` `--only` `--types` `--brands` `--loose` |
| `local_ingest.py` | **사내에 이미 있는 PDF·엑셀에서 스펙 추출** |
| `organize_catalogs.py` | 받은 카달로그를 지역 → 브랜드 폴더로 정리 + 목록 CSV·HTML |
| `summary.py` | 수집 결과를 한 화면에 요약 |
| `run_menu.py` | 더블클릭용 한글 메뉴 (0~8번) |
| `buyer_html_to_csv.py` | 사내 시스템 HTML 의 gzip+base64 바이어 DB 해제 |
| `gb_japan_to_catalog_csv.py` | 사내 일본 제품 엑셀 → 임포트 CSV |
| `dump_seed.js` + `build_workbook.py` | 앱 시드 → 엑셀 워크북 |

### 데이터 — `data/`

- `catalog_sources.csv` — 카달로그 **실제 주소** 77건. 대표 홈페이지가 아니라
  PDF 직링크·제품목록 주소. `검색확인`(35) 과 `패턴추정`(40) 을 컬럼으로 구분
- `distributor_sources.csv` — 유통사 44곳 · 취급 브랜드 · 브랜드별 URL 패턴

---

## 3부 — 조사에서 나온 것

### 바이어를 되짚어 알아낸 것

- **AAP = Alliance Automotive Group UK & IE.** NAPA UK · Tayna(2023 인수) ·
  Platinum Batteries(2018 인수) · FPS 가 전부 한 그룹. 영국 배터리 채널의 관문
- **YBS / YBIT = GS Yuasa 유럽 판매법인** (YBF 리옹 · YBIB 마드리드 · YBEU 뒤셀도르프)
- **MEBCO(사우디)** — Clarios 49% + 자밀·알조마이 등 51% 합작. 아랍권 최대
  납축전지 공장(연 340만 개). **중동 ACDelco 를 여기서 만든다.** 자체 브랜드
  MEBCO·PIONEER·BLACK BOX 를 GCC·이집트·시리아·레바논·예멘·요르단으로 수출
- **Recor(그리스)** — 코모티니 공장, 1964년, 그리스 유일의 스타터배터리 생산자
- **Roussakis(그리스)** — ACDelco·Yuasa·GS·Trojan 을 한 진열대에. 브랜드별 URL 규칙적
- **Nova Power Tech(인도)** — Amaron 유통 + 계열 브랜드 Mantra
- **Bosch 가 25개국 이상에서 바이어로 등장** — 아프리카·중남미·동남아 국가별 포털

### 값어치 있는 카달로그

- **Yuasa NZ · Century AU 크로스레퍼런스 차트** — 경쟁사가 자기 품번을 우리 규격에
  어떻게 매핑하는지가 그대로 적혀 있다. 대체품 표를 통째로 얻는 셈
- **SuperCharge 158쪽 피트먼트 가이드**
- **Exide Technical Guide Edition 5**
- **토요타 북미 OEM 배터리 카달로그**

### 끝내 못 찾은 것

`TRANS PARTS(AUTO)` · `M/S NICKEL GLASS AND BATTERIES` · 러시아 Rocket 채널.
사명이 너무 일반적이라 공개 검색으로 특정이 안 됐다. 추정으로 채우지 않고 남겼다.
사내 오더 이력의 인보이스 주소를 보면 즉시 확정된다.

`Marathon` (사내자료의 `MKTG33340 Marathon EFB Trade Sheet`) 도 어느 회사
브랜드인지 확정하지 못해 DB 에 넣지 않았다.

---

## 4부 — 틀렸다가 고친 것

여기가 이 문서의 알맹이다. 전부 실제로 겪은 것이고, 대부분 내가 만든 문제다.

### 데이터 파싱

**357쪽 카달로그에서 스펙 4건.** BCI 규격을 `group size 24` 처럼 그 단어가
있을 때만 인식하고 있었다. 그런데 카달로그 표에는 그 단어가 없다 —
`S4 24F-525B  24F  525  650` 처럼 코드만 온다. 북미·호주 카달로그가 통째로
새고 있었다. 실재하는 BCI 그룹 목록으로 대조하도록 고쳤다. 글자 붙은 코드
(24F·94R·8D·H6)는 바로 받고, 맨숫자(24·65)는 한 줄에 두 번 나오거나
품번꼴(`65-750B`)일 때만 받는다. 안 그러면
`Table 27 shows results for 2023` 이 BCI 27 이 된다.
**결과: 68건 → 380건 (5.6배), BCI 136건 신규 확보.**

**`750 CCA` 를 못 읽었다.** `CCA 750` 만 보고 있었는데 현장 표기는 숫자가 먼저
오는 쪽이 더 흔하다.

**`D23` 을 못 읽었다.** `75D23L` 만 잡아서 용량 접두가 없는 JIS 그룹 표기가
샜다. 일본·동남아 카달로그가 여기 걸린다.

**`100Ah` 를 CCA 100 으로 읽었다.** `(\d{3,4})\s*A(?![a-z])` — 부정 선읽기가 없었다.

**단위 없는 표 숫자를 추측하지 않기로 했다.** `24F 525 650 70 18.6` 에서
어느 게 CCA 고 어느 게 RC 인지는 카달로그마다 열 순서가 다르다. 추측해서 채우면
틀린 값이 사실로 박힌다. Ah·CCA 칸은 비우고 숫자를 원문 그대로 남긴다.

**PDF 표의 칸이 줄마다 쪼개지는 문서가 있다.** 줄 단위 파싱이 거의 0건이면 행을
되붙여 다시 훑는다. 단 이웃 줄을 무턱대고 붙이면 남의 행이 섞이므로 짧고 숫자를
품은 줄만 잇는다.

**엑셀을 정규식으로 긁어 0건이 나왔다.** 엑셀은 열 제목이 있는데 그걸 안 쓰고
행을 문자열로 이어 붙여 훑었다. 표에 단위가 없으니 당연히 0건이다.
제목(`품번 | 규격 | C20 | CCA`)을 읽어 열을 짚도록 고쳤다.

**사내 엑셀에 오타 셀이 섞여 통계가 망가졌다.** C20 칸에 `1452` 가 있어서
"D23 용량 57~1452Ah" 가 나왔다. 값 범위 가드를 넣었다.

### 앱

**localStorage 가 15,000행에서 터졌다.** `QuotaExceededError` → 새로고침에 전부
소실. 임포트 행을 IndexedDB 로 옮겼다.

**검색창의 `change` 이벤트가 클릭 대상을 파괴했다.** 타이핑 → 행 클릭 → blur →
`change` → 재렌더 → 그 행이 사라짐 → 클릭이 허공을 친다. 텍스트 입력은 `oninput`.

**세계지도가 산점도로 보였다.** 대륙 윤곽 폴리곤을 넣고, 버블 반지름을 건수의
제곱근에 비례하게 하고, 라벨 충돌을 회피했다.

**`file://` 에는 `crypto.subtle` 이 없다.** 폰에 저장해서 여는 게 주 사용
시나리오인데 SHA-256 이 안 돌았다. 순수 JS 폴백을 넣고 NIST 벡터 3개로 검증했다.

**16,000행을 한 번에 DOM 에 그려 멈췄다.** 300행 페이징 + "더 보기".

**폰에서 가로 스크롤이 생겼다.** `.g2` 가 좁은 화면에서도 2열로 남아 있었다.

### 엑셀 워크북

**`MAXIFS` 는 구버전 엑셀에서 `#NAME?`.** `_xlfn.` 접두가 붙는다.
`SUMPRODUCT(MAX((조건)*(범위)))` 로 바꿨다.

**파이썬 `round()` 와 엑셀 `ROUND` 가 달라 없는 버그를 쫓았다.** 은행가 반올림
vs 반올림 올림. 712 vs 713 로 갈렸다. 검증기는 `int(x+0.5)`.

### 윈도우 전달 — 여기서 가장 많이 막혔다

**배치파일이 통째로 깨졌다.** 한글 윈도우 `cmd` 는 `.bat` 를 CP949 로 읽는다.
UTF-8 로 저장한 배치에 한글을 넣으면 바이트가 깨지면서 파서가 줄 중간을
잘라먹는다 — `echo` 가 `ho` 로, `catalog_registry.csv` 가 `g_registry.csv` 로
잘렸다. `chcp 65001` 을 앞에 둬도 이미 파싱이 시작된 뒤라 소용없다.
→ **배치는 순수 ASCII 로 파이썬만 찾고, 한글 안내와 실제 일은 파이썬이 한다.**
파이썬은 콘솔에 유니코드를 직접 쓰기 때문에 코드페이지와 무관하다.

**zip 이 윈도우에서 안 풀렸다.** 리눅스 `zip` 명령은 한글 파일명에 UTF-8
플래그(범용 비트 11, 0x800)를 안 단다. 탐색기는 그 플래그가 없으면 이름을
CP949 로 읽고 "압축 폴더가 올바르지 않습니다" 로 거부한다.
→ 파이썬 `zipfile` 로 구우면 비ASCII 이름에 그 플래그가 자동으로 붙는다.

**CSV 가 엑셀에서 전부 깨졌다.** `catalog_sources.csv` 에 BOM 이 없었다.
한글 엑셀은 BOM 이 없으면 UTF-8 을 CP949 로 읽는다.
`distributor_sources.csv` 는 BOM 이 있어서 멀쩡했는데 그 차이를 못 봤다.
→ `data/*.csv` 를 UTF-8 BOM + CRLF 로 통일.

**요약 표의 한글 줄이 어긋났다.** 한글은 콘솔에서 두 칸을 차지하는데 `%-16s` 는
글자 수로 맞춘다. `east_asian_width` 로 실제 표시 폭을 세도록 고쳤다.

**8번(엑셀 읽기)이 조용히 0건을 냈다.** 메뉴가 `requests` 와 `pypdf` 만 깔고
`openpyxl` 을 빠뜨렸다. 설치 목록에 넣고, 없으면 스스로 한 번 깔아보게 했다.

**`pypdf` 가 깨진 PC 에서 수집 전체가 멈출 수 있었다.** `ImportError` 만 잡고
있었는데 실제로 올라오는 건 `pyo3 PanicException` 이고, 이건 `Exception` 이
아니라 `BaseException` 상속이라 안 잡힌다. 본문 추출을 못 하더라도 PDF 를 받아
해시·쪽수를 남기는 일은 계속돼야 한다.

### 안내 자체가 틀린 것

**"1번을 돌리면 `collect_log.csv` 가 생긴다" 고 안내했는데 안 생긴다.**
`--dry` 는 주소 목록을 화면에 뿌리기만 하고 네트워크 요청도 파일 생성도 안 한다.
"주소가 살아있는지 확인" 이라는 기능이 **애초에 없었다.**
→ `--check` 를 새로 만들었다. 파일은 안 받고 주소만 두드리고, HEAD 를 막는
서버가 많으니 403·405 면 GET 으로 한 번 더 보고, 끝에 판정을 직접 찍는다.

**"몇 건 나왔냐" 를 사용자에게 물었다.** CSV 를 열어 행을 세게 만든 게 잘못이다.
→ `summary.py` (메뉴 7번) 가 결과를 한 화면에 답한다.

**말투.** README 에 쓰는 간결한 지시형("~해라", "~하지 마라")을 사람과 대화할
때까지 그대로 썼다. 문서에서 읽을 때와 대화로 들을 때는 완전히 다르게 느껴진다.

---

## 5부 — 사내자료 대조

사용자가 사내에 모아둔 카달로그를 추가했다. 내가 조사한 것과 대조한 결과:

| 브랜드 | 제조사 DB | 브랜드×마켓 | 내가 가진 주소 |
|---|---|---|---|
| Amaron · Banner · MOLL · FIAMM · Camel · Leoch · Exide | O | O | 있음 |
| Marathon | **없음** | **없음** | **없음** |

**브랜드는 8개 중 7개 커버. 그런데 문서 자체는 거의 다 내가 못 가진 것이었다.**

- 정확히 겹치는 건 하나 — `NEW_Exide_Edition5_TechnicalGuid.pdf`
- 나머지는 내가 가진 게 제품목록 웹페이지뿐이고 사내에는 실물 PDF 가 있었다
- 결정적으로 주소 확인에서 **Banner 404 · FIAMM 404 · Leoch 404 · Camel 연결실패** —
  내 주소는 죽어 있었는데 사내에는 실물이 있었다

**교훈: 사내에 이미 있는 자료를 먼저 훑어야 했다.** 웹에서 주소를 찾는 것보다
빠르고 정확하다. 그래서 `local_ingest.py` (메뉴 8번) 를 만들었다.

---

## 6부 — 지금 상태

### 데이터

| | |
|---|---|
| 규격 마스터 | 49종 |
| 모델(시드) | 862 — `pub` 69 · `est` 97 · **`slot` 696 (81%)** |
| 자동 추출 스펙 | **380건** — 높음+보통 64 · 낮음 316 |
| 제조사·유통사 | 74 |
| 브랜드 × 마켓 | 76 |
| 유통사 | 44 |
| 차량 적합성 | 77 |
| 카달로그 주소 | 77 (검색확인 35 · 패턴추정 40) |
| 사내망 주소 확인 | 열림 37 · 막힘 48 |

### 검증한 것 / 못 한 것

- 앱 11개 화면 렌더·필터·정렬·상세·CSV 임포트·모바일 레이아웃 — 헤드리스
  크로미움으로 JS 에러 0 확인
- 사내 일본 엑셀 794행, 바이어 DB 16,196행 임포트 후 새로고침 잔존 확인
- 워크북 12시트 생성 확인. 수식 로직은 파이썬으로 복제해 앱 값과 대조
- **엑셀 재계산은 실제로 못 돌렸다** — 작업 환경에 LibreOffice 가 안 뜬다.
  Excel 2007 세대 함수만 쓰고 `fullCalcOnLoad` 를 켜뒀다

### 남은 일

1. **`slot` 696건을 실측으로 덮기** — 카달로그를 받아 스펙으로 대체
2. **확신도 `낮음` 316건의 열 매핑** — 카달로그별 표 열 순서를 한 번씩 지정하면
   일괄로 채울 수 있다. 도구를 만들 수 있다
3. **`패턴추정` 40건 중 404 난 것을 실주소로 교체**
4. **사내자료 8번 결과 확인** — openpyxl 설치 후 재실행 필요
5. Marathon 브랜드 소속 확인
6. 못 찾은 바이어 3건 — 사내 인보이스 주소로 확정
7. `buyer_unmapped.csv` 358곳 국가 채우기

---

## 부록 — 어느 환경에서 무엇이 되나

| 하려는 것 | 필요한 것 |
|---|---|
| 앱 보기·고치기 | 브라우저만. 인터넷도 불필요 |
| 카달로그 받기 | 파이썬 + **차단되지 않은 인터넷**. 회사 PC·집 PC·노트북 아무거나 |
| 사내자료 읽기 | 파이썬 + openpyxl. 인터넷 불필요 |
| 카달로그 1~2건 수동 등록 | **폰만.** 앱에서 PDF 를 고르면 브라우저가 해시·쪽수·판을 계산 |
| 엑셀 다시 굽기 | Node.js + 파이썬(openpyxl) |

클라우드·샌드박스 작업 환경은 제조사 도메인을 거의 다 막는다(403 CONNECT).
그래서 이 프로젝트의 산출물은 "완성된 DB" 가 아니라
**"수집 엔진 + 검증된 URL 레지스트리"** 이고, 실제 수집은 사용자 환경에서 돈다.
